
  # Portfolio design for engineer

  A portfolio template built with React, Tailwind CSS, and shadcn/ui.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  